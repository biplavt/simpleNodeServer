<!DOCTYPE html>
<html>
	<head>
		<meta charset="utf-8">
		<title> {{pageTitle}}</title>
	</head>
	<body>
		{{>header}}
		<h2>{{welcomeMessage}}</h2>
		<p>  Some text </p>

		{{> footer}}
	</body>
</html>